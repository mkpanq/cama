export async function GET(request: Request) {
  // Means we are ready to go
  // Ask API for requisition details
  // Save requisition to DB
  // Ask for accounts
  // Save accounts to DB
  // Start downloading all accounts data to DB - transaction, details, balances etc
}
